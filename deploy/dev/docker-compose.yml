name: ling_dev

services:
  ling-server:
    restart: always
    build:
      context: ../..
      dockerfile: deploy/images/Dockerfile.server
    image: ling-server-dev:v1.0
    container_name: ling-dev-server
    env_file:
      - ${LING_COMPOSE_ENV_FILE:-.env}
    ports:
      - 31080:8000
    volumes:
      - ${LING_DEPLOY_ROOT:-../data/dev}/data:/app/data
      - ${LING_DEPLOY_ROOT:-../data/dev}/logs:/app/logs
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ling-api/health', timeout=5)",
        ]
      interval: 30s
      timeout: 5s
      retries: 5
      start_period: 20s
    mem_limit: 2g
    cpus: 1.0
